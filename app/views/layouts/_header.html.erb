<nav class="navbar navbar-default" role="navigation">
  <div class="container">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <a class="navbar-brand" href="/">Status</a>
</div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Friends", root_path %></li>
        <li><%= link_to "Invite", invite_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to "Account Settings", edit_user_registration_path %></li>
          <li><span class="btn btn-default navbar-btn">
            <%= link_to new_stati_path do %>
              <span class="glyphicon glyphicon-plus"></span> Update Status
            <% end %></span></button>
          <li> <%= link_to "Log out", destroy_user_session_path, method: :delete %> </li>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</div>

</nav>