<h1>What's Your Status?</h1>

<div class="container">
<table class="table table-hover table-boardered">
      <thead>
        <tr>
          <th>Name</th>
          <th>Status</th>
          <th>Distance</th>
          <th>Actions</th>
        </tr>
      </thead>


  <tbody>
    <% @statis.each do |stati| %>
      <tr>
        <td><%= stati.description %></td>
        <td> </td>
        <td> </td>
        <td><%= link_to 'Show', stati %> <%= link_to 'Edit', edit_stati_path(stati) %> <%= link_to 'Destroy', stati, method: :delete, data: { confirm: 'Are you sure?' } %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Update Status', new_stati_path %>
