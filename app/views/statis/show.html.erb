<p id="notice"><%= notice %></p>

<div class="container">
<table class="table table-hover table-boardered">
      <thead>
        <tr>
          <th>Name</th>
          <th>Status</th>
          <th>Distance</th>
        </tr>
      </thead>
      <tbody>
        <tr class="success">
          <td><%= @stati.user.email %></td>
          <td><%= @stati.description %></td>
          <td>2 miles</td>
        </tr>

</div>
<% if @statis_user == current_user %>
<%= link_to 'Edit', statis_path %> | <% end %>
<%= link_to 'Back', statis_path %>

